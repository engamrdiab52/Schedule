package com.afdal.schedule

import android.content.Context
import org.junit.Before

class FirebaseServiceAndroidTest {
    private lateinit var context: Context
    @Before
    fun setUp(){
      /*  context = ApplicationProvider.getApplicationContext()
        val fileName = "test File"
        val localPrivateFile = File(context.filesDir, fileName)*/
    }
    fun createFile_create_returnFile() {
    //given
       // FirebaseService.createFile()
    }
}